# 이 파일을 레포의 frontend/nginx.conf 로 저장
# React Router 등 클라이언트 사이드 라우팅 사용 시 새로고침해도 404 안 나도록 처리
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
