# PostgreSQL
POSTGRES_DB=promptstudio
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# OpenAI
# platform.openai.com에서 발급받은 API 키를 입력합니다. 따옴표는 붙이지 않습니다.
# 모델 ID는 비워두면 아래 기본값이 적용됩니다.
OPENAI_API_KEY=
OPENAI_CODE_MODEL=gpt-5.6-luna
OPENAI_FEEDBACK_MODEL=gpt-5.4-mini

# 문제 저장소 (GitLab)
# lab.ssafy.com에서 발급받은 read_api 이상 권한의 액세스 토큰을 입력합니다.
# 토큰 외에는 비워두면 아래 기본값이 적용됩니다.
PROBLEM_REPO_TOKEN=
PROBLEM_REPO_BASE_URL=https://lab.ssafy.com
PROBLEM_REPO_PROJECT_ID=1419080
PROBLEM_REPO_BRANCH=master
PROBLEM_SYNC_ENABLED=true
PROBLEM_SYNC_INTERVAL_MS=300000

# Redis
REDIS_PASSWORD=

# TURN (릴레이 게임 WebRTC)
# 로컬 개발에서는 coturn 없이도 동작합니다(같은 호스트끼리는 직결). 배포에서 채웁니다.
# TURN_EXTERNAL_IP는 서버의 공인 IP — 없으면 relay 후보에 사설 IP가 실려 외부에서 도달하지 못합니다.
TURN_USER=relay
TURN_PASSWORD=
TURN_EXTERNAL_IP=
# 백엔드가 클라이언트에 내려주는 ICE 설정. 비우면 STUN(구글 공용)만 내려갑니다.
# 배포 예: RELAY_TURN_URLS=turn:i15a505.p.ssafy.io:3478
RELAY_TURN_URLS=

# RabbitMQ
RABBITMQ_USER=rabbitmq
RABBITMQ_PASSWORD=

# 로그 파일 경로 (선택)
# 지정하지 않으면 backend/logs/backend.log 에 쌓입니다(application.yml 기본값).
# 로컬에서는 건드릴 필요가 없습니다. 다른 위치에 쌓고 싶을 때만 주석을 풀어 지정하세요.
# 배포는 이 파일을 읽지 않습니다 — docker-compose.backend.yml 이 컨테이너 경로를 직접 지정합니다.
#LOG_FILE=./logs/backend.log

# 프론트엔드 (backend endpoint)
# 비워두면 vite 프록시(/api)를 통해 로컬 백엔드로 요청합니다.
VITE_API_BASE_URL=
